import typemutation from './mutation';
import typeQuery from './query';
import { resolvers } from './resolver';

import type from './type';

const User = {typemutation , typeQuery ,resolvers , type};
export default User;